package com.example.familymap.utils

object SuopConstants {
    //A string containing characters that are unlikely to ever be found naturally in that order, allowing the usage of string.split()
    const val STRING_SEPARATOR = "§█Ä§"
}